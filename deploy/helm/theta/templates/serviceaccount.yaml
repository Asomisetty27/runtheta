{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "theta.serviceAccountName" . }}
  labels:
    {{- include "theta.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
